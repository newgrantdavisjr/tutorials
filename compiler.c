#include <stdio.h>
/*Explanations for compiler - clang/gcc*/
int main()
{
  printf("Ther are four stages to compilation\n");
  printf("Preprocessor should and can be done when source code written\n");
  printf("Compiler accepts preprocess code and begins the assembler language");
  printf("Compiler passes parsed assembled syntax to the assembler");
  printf("The assembler passes code to linker for standard library\n ");
  return 0;
}
